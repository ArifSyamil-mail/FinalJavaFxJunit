package com.example.finaljavafxjunit;

public interface ApplicationLauncher {
    //This is an interface that declares a single method launch(String[] args). The purpose is to abstract away the launching of the JavaFX application.
    void launch(String [] args);
}
